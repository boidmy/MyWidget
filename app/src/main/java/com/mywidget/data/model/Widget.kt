package com.mywidget.data.model

import android.content.Context
import com.mywidget.MainApplication
import com.mywidget.data.room.User

class Widget {

    /*fun userList(context: Context): List<User> {
        val r = Runnable {
            try {
                var userList = MainApplication.INSTANSE.userDb?.userDao()?.getUser()!!
            } catch (e: java.lang.Exception) {

            }
        }

        val thread = Thread(r)
        thread.start()
        return
    }*/
}